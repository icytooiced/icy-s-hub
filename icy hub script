--// SERVICES
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local GuiService = game:GetService("GuiService")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer

--// CONFIG
local SAVE_KEY_FILE = "icys_key.txt"

--// VALID KEYS
local ValidKeys = {
	["55865349"] = true,
	["99575032"] = true,
	["53320942"] = true,
	["92378629"] = true,
	["76139575"] = true,
	["vetements"] = true,
	["icysprivatekey"] = true
}

--------------------------------------------------------------------
-- SAVE / LOAD KEY
--------------------------------------------------------------------

local function SaveKey(key)
	if writefile then
		writefile(SAVE_KEY_FILE, key)
	end
end

local function LoadKey()
	if readfile and isfile and isfile(SAVE_KEY_FILE) then
		return readfile(SAVE_KEY_FILE)
	end
end

--------------------------------------------------------------------
-- TWEEN HELPER
--------------------------------------------------------------------

local function Tween(obj, props, time)
	return TweenService:Create(
		obj,
		TweenInfo.new(time or 0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
		props
	)
end

--------------------------------------------------------------------
-- TEXT ANIMATION
--------------------------------------------------------------------

local function AnimateText(label)
	task.spawn(function()
		while label.Parent do
			Tween(label,{TextColor3 = Color3.fromRGB(0,170,255)},1):Play()
			task.wait(1)
			Tween(label,{TextColor3 = Color3.fromRGB(180,220,255)},1):Play()
			task.wait(1)
		end
	end)
end

--------------------------------------------------------------------
-- KEY GUI
--------------------------------------------------------------------

local KeyGui = Instance.new("ScreenGui")
KeyGui.Name = "KeyGui"
KeyGui.ResetOnSpawn = false
KeyGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
KeyGui.DisplayOrder = 999
KeyGui.Parent = CoreGui

local KeyFrame = Instance.new("Frame", KeyGui)
KeyFrame.Size = UDim2.fromOffset(360,220)
KeyFrame.Position = UDim2.fromScale(0.5,0.5)
KeyFrame.AnchorPoint = Vector2.new(0.5,0.5)
KeyFrame.BackgroundColor3 = Color3.fromRGB(20,20,26)
KeyFrame.Active = true
KeyFrame.Draggable = true
Instance.new("UICorner", KeyFrame).CornerRadius = UDim.new(0,18)

local stroke = Instance.new("UIStroke", KeyFrame)
stroke.Color = Color3.fromRGB(0,170,255)
stroke.Transparency = 0.5

local Title = Instance.new("TextLabel", KeyFrame)
Title.Size = UDim2.new(1,0,0,40)
Title.BackgroundTransparency = 1
Title.Text = "Enter Key"
Title.Font = Enum.Font.GothamBold
Title.TextSize = 26
Title.TextColor3 = Color3.fromRGB(0,170,255)
AnimateText(Title)

local KeyBox = Instance.new("TextBox", KeyFrame)
KeyBox.Size = UDim2.fromScale(0.8,0.22)
KeyBox.Position = UDim2.fromScale(0.1,0.38)
KeyBox.PlaceholderText = "Key here..."
KeyBox.Text = ""
KeyBox.Font = Enum.Font.Gotham
KeyBox.TextSize = 18
KeyBox.TextColor3 = Color3.new(1,1,1)
KeyBox.BackgroundColor3 = Color3.fromRGB(35,35,40)
Instance.new("UICorner", KeyBox)

local DiscordLabel = Instance.new("TextButton", KeyFrame)
DiscordLabel.Size = UDim2.fromScale(1,0.2)
DiscordLabel.Position = UDim2.fromScale(0,0.62)
DiscordLabel.BackgroundTransparency = 1
DiscordLabel.Text = "Key in the Discord server\ndiscord.gg/bgX8GE6NSN"
DiscordLabel.Font = Enum.Font.Gotham
DiscordLabel.TextSize = 14
DiscordLabel.TextColor3 = Color3.fromRGB(0,170,255)
DiscordLabel.TextWrapped = true

DiscordLabel.MouseButton1Click:Connect(function()
	pcall(function()
		GuiService:OpenBrowserWindow("https://discord.gg/bgX8GE6NSN")
	end)
end)

local Status = Instance.new("TextLabel", KeyFrame)
Status.Size = UDim2.new(1,0,0,20)
Status.Position = UDim2.fromScale(0,0.88)
Status.BackgroundTransparency = 1
Status.Font = Enum.Font.GothamBold
Status.TextSize = 16

--------------------------------------------------------------------
-- MAIN GUI
--------------------------------------------------------------------

local MainGui = Instance.new("ScreenGui")
MainGui.Name = "IcysHub"
MainGui.Enabled = false
MainGui.ResetOnSpawn = false
MainGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
MainGui.DisplayOrder = 999
MainGui.Parent = CoreGui

local MainFrame = Instance.new("Frame", MainGui)
MainFrame.Size = UDim2.fromOffset(470,350)
MainFrame.Position = UDim2.fromScale(0.5,0.5)
MainFrame.AnchorPoint = Vector2.new(0.5,0.5)
MainFrame.BackgroundColor3 = Color3.fromRGB(20,20,26)
MainFrame.Active = true
MainFrame.Draggable = true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0,18)

local mainStroke = Instance.new("UIStroke", MainFrame)
mainStroke.Color = Color3.fromRGB(0,170,255)
mainStroke.Transparency = 0.4

local Header = Instance.new("TextLabel", MainFrame)
Header.Size = UDim2.new(1,0,0,40)
Header.BackgroundTransparency = 1
Header.Text = "Icys Hub"
Header.Font = Enum.Font.GothamBold
Header.TextSize = 24
Header.TextColor3 = Color3.fromRGB(0,170,255)
AnimateText(Header)

local Close = Instance.new("TextButton", MainFrame)
Close.Text = "X"
Close.Size = UDim2.fromOffset(30,30)
Close.Position = UDim2.new(1,-35,0,5)
Close.BackgroundColor3 = Color3.fromRGB(190,60,60)
Close.TextColor3 = Color3.new(1,1,1)
Instance.new("UICorner", Close)

Close.MouseButton1Click:Connect(function()
	MainGui:Destroy()
end)

local Minimize = Instance.new("TextButton", MainFrame)
Minimize.Text = "-"
Minimize.Size = UDim2.fromOffset(30,30)
Minimize.Position = UDim2.new(1,-70,0,5)
Minimize.BackgroundColor3 = Color3.fromRGB(60,60,70)
Minimize.TextColor3 = Color3.new(1,1,1)
Instance.new("UICorner", Minimize)

local ButtonHolder = Instance.new("Frame", MainFrame)
ButtonHolder.Size = UDim2.new(1,-20,1,-60)
ButtonHolder.Position = UDim2.new(0,10,0,50)
ButtonHolder.BackgroundTransparency = 1

local minimized = false

Minimize.MouseButton1Click:Connect(function()
	minimized = not minimized
	if minimized then
		Tween(MainFrame,{Size = UDim2.fromOffset(470,60)},0.25):Play()
		ButtonHolder.Visible = false
	else
		Tween(MainFrame,{Size = UDim2.fromOffset(470,350)},0.25):Play()
		task.wait(0.1)
		ButtonHolder.Visible = true
	end
end)

--------------------------------------------------------------------
-- LOADING NOTIFICATION
--------------------------------------------------------------------

local function ShowLoading(text)
	local loadFrame = Instance.new("Frame", MainGui)
	loadFrame.Size = UDim2.fromOffset(260,90)
	loadFrame.Position = UDim2.fromScale(0.5,0.9)
	loadFrame.AnchorPoint = Vector2.new(0.5,0.5)
	loadFrame.BackgroundColor3 = Color3.fromRGB(20,20,26)
	Instance.new("UICorner", loadFrame)

	local stroke = Instance.new("UIStroke", loadFrame)
	stroke.Color = Color3.fromRGB(0,170,255)

	local label = Instance.new("TextLabel", loadFrame)
	label.Size = UDim2.new(1,0,1,0)
	label.BackgroundTransparency = 1
	label.Text = "Loading "..text.."..."
	label.Font = Enum.Font.GothamBold
	label.TextSize = 18
	label.TextColor3 = Color3.fromRGB(0,170,255)

	task.delay(2,function()
		loadFrame:Destroy()
	end)
end

--------------------------------------------------------------------
-- BUTTONS
--------------------------------------------------------------------

local names = {
	"ESP","Aimbot","Admin","Fly","Speed","Anti-AFK","Anti-VC-Ban","Quiz Bot","Rivals"
}

local urls = {
	"https://raw.githubusercontent.com/icytooiced/icy-s-hub/refs/heads/main/esp",
	"https://raw.githubusercontent.com/icytooiced/icy-s-hub/refs/heads/main/aimbot",
	"https://raw.githubusercontent.com/ltseverydayyou/Nameless-Admin/main/Source.lua",
	"https://raw.githubusercontent.com/icytooiced/icy-s-hub/refs/heads/main/fly",
	"https://raw.githubusercontent.com/icytooiced/icy-s-hub/refs/heads/main/speed%20script",
	"https://raw.githubusercontent.com/icytooiced/icy-s-hub/refs/heads/main/anti-afk",
	"https://raw.githubusercontent.com/icytooiced/icy-s-hub/refs/heads/main/anti%20vc%20ban",
	"https://raw.githubusercontent.com/Damian-11/quizbot/master/quizbot.luau",
	"https://raw.githubusercontent.com/prostone55/NitoHub/refs/heads/main/RivalsMain.lua"
}

for i=1,#names do
	local b = Instance.new("TextButton", ButtonHolder)
	b.Size = UDim2.fromOffset(140,36)
	b.Position = UDim2.new(0.05 + ((i-1)%3)*0.3,0,0.02 + math.floor((i-1)/3)*0.18,0)
	b.Text = names[i]
	b.Font = Enum.Font.GothamBold
	b.TextSize = 18
	b.TextColor3 = Color3.new(1,1,1)
	b.BackgroundColor3 = Color3.fromRGB(40,40,50)
	Instance.new("UICorner", b)

	b.MouseButton1Click:Connect(function()
		ShowLoading(names[i])
		task.wait(2)
		loadstring(game:HttpGet(urls[i]))()
	end)
end

--------------------------------------------------------------------
-- KEY CHECK
--------------------------------------------------------------------

local function Login(key)
	if ValidKeys[key] then
		SaveKey(key)
		KeyGui:Destroy()
		MainGui.Enabled = true
	else
		Status.Text = "Invalid Key"
		Status.TextColor3 = Color3.fromRGB(255,80,80)
	end
end

KeyBox.FocusLost:Connect(function(enterPressed)
	if enterPressed then
		Login(KeyBox.Text:lower():gsub("%s+",""))
	end
end)

local saved = LoadKey()
if saved and ValidKeys[saved] then
	KeyGui:Destroy()
	MainGui.Enabled = true
end
