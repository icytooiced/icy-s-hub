--// SERVICES
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local GuiService = game:GetService("GuiService")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

--// VALID KEYS
local ValidKeys = {
	["6631044286"] = true,
	["6232749547"] = true,
	["7681995313"] = true,
	["0899694468"] = true,
	["xyra"] = true
}

--// TWEEN HELPER
local function Tween(obj, props, t)
	return TweenService:Create(
		obj,
		TweenInfo.new(t, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
		props
	)
end

--// =========================
--// SECURITY SCAN (BLOCKING)
--// =========================
do
	local ScanGui = Instance.new("ScreenGui", PlayerGui)
	ScanGui.Name = "SecurityScan"

	local Frame = Instance.new("Frame", ScanGui)
	Frame.Size = UDim2.fromOffset(320,70)
	Frame.Position = UDim2.new(0.5,-160,0,20)
	Frame.BackgroundColor3 = Color3.fromRGB(30,30,30)
	Frame.BorderSizePixel = 0
	Instance.new("UICorner", Frame)

	local Label = Instance.new("TextLabel", Frame)
	Label.Size = UDim2.new(1,0,0.5,0)
	Label.BackgroundTransparency = 1
	Label.Text = "Protected by Icy Security v1.4.2"
	Label.Font = Enum.Font.GothamBold
	Label.TextSize = 16
	Label.TextColor3 = Color3.new(1,1,1)

	local BarBG = Instance.new("Frame", Frame)
	BarBG.Position = UDim2.fromScale(0.05,0.6)
	BarBG.Size = UDim2.fromScale(0.9,0.25)
	BarBG.BackgroundColor3 = Color3.fromRGB(50,50,50)
	Instance.new("UICorner", BarBG)

	local Bar = Instance.new("Frame", BarBG)
	Bar.Size = UDim2.fromScale(0,1)
	Bar.BackgroundColor3 = Color3.fromRGB(0,170,255)
	Instance.new("UICorner", Bar)

	-- BLOCKING PROGRESS (IMPORTANT)
	local start = tick()
	while tick() - start < 4 do
		local p = math.clamp((tick()-start)/4,0,1)
		Bar.Size = UDim2.fromScale(p,1)
		RunService.RenderStepped:Wait()
	end

	-- Run fake scan
	pcall(function()
		loadstring(game:HttpGet(
			"https://raw.githubusercontent.com/icytooiced/icy-s-hub/refs/heads/main/security%20scan"
		))()
	end)

	ScanGui:Destroy()
end

-- =========================
-- KEY GUI (CREATED AFTER SCAN)
-- =========================
local KeyGui = Instance.new("ScreenGui", PlayerGui)
KeyGui.Name = "KeyGui"

local KF = Instance.new("Frame", KeyGui)
KF.Size = UDim2.fromOffset(300,200)
KF.Position = UDim2.fromScale(0.5,0.5) - UDim2.fromOffset(150,100)
KF.BackgroundColor3 = Color3.fromRGB(25,25,25)
KF.Active = true
KF.Draggable = true
Instance.new("UICorner", KF)

local Title = Instance.new("TextLabel", KF)
Title.Size = UDim2.new(1,0,0,40)
Title.BackgroundTransparency = 1
Title.Text = "Enter Key"
Title.Font = Enum.Font.GothamBold
Title.TextSize = 22
Title.TextColor3 = Color3.new(1,1,1)

local Box = Instance.new("TextBox", KF)
Box.Size = UDim2.fromScale(0.8,0.25)
Box.Position = UDim2.fromScale(0.1,0.35)
Box.PlaceholderText = "Key here..."
Box.BackgroundColor3 = Color3.fromRGB(45,45,45)
Box.TextColor3 = Color3.new(1,1,1)
Instance.new("UICorner", Box)

local Discord = Instance.new("TextButton", KF)
Discord.Size = UDim2.fromScale(1,0.15)
Discord.Position = UDim2.fromScale(0,0.65)
Discord.BackgroundTransparency = 1
Discord.Text = "Key in Discord: discord.gg/bgX8GE6NSN"
Discord.Font = Enum.Font.Gotham
Discord.TextSize = 14
Discord.TextColor3 = Color3.fromRGB(0,170,255)
Discord.TextWrapped = true
Discord.MouseButton1Click:Connect(function()
	GuiService:OpenBrowserWindow("https://discord.gg/bgX8GE6NSN")
end)

local Status = Instance.new("TextLabel", KF)
Status.Size = UDim2.new(1,0,0,24)
Status.Position = UDim2.fromScale(0,0.85)
Status.BackgroundTransparency = 1
Status.Font = Enum.Font.GothamBold
Status.TextSize = 16
Status.TextColor3 = Color3.fromRGB(255,0,0)

-- =========================
-- KEY CHECK
-- =========================
Box.FocusLost:Connect(function(enter)
	if not enter then return end
	local k = Box.Text:gsub("%s+","")

	if k:lower() == "naruto" then
		LocalPlayer:Kick("fuck you")
		return
	end

	if ValidKeys[k] then
		KeyGui:Destroy()
	else
		Status.Text = "Invalid Key"
		Box.Text = ""
	end
end)

